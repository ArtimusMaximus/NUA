HOSTNAME=
PORT=
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CREDENTIAL_ENCRYPTION_KEY=

# --- Optional authentication (disabled by default) ---
# Set NUA_AUTH_ENABLED=true (or just provide both credentials) to require a
# login before the web UI / API can be used.
NUA_AUTH_ENABLED=false
NUA_AUTH_USERNAME=
NUA_AUTH_PASSWORD=
# Optional session lifetime in milliseconds (default 8 hours = 28800000)
NUA_AUTH_SESSION_TTL_MS=28800000
